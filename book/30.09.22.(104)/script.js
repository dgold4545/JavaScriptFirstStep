'use strict';

let str = '12345';

if (str.length >= 3) {
    console.log('!');
}

let arr = [12, 32, 323, 41341, 324];
if (arr.length == 5) {
    console.log(arr.length);
    let dima = arr[0] + arr[1] + arr[2] + arr[3] + arr[4];
    console.log(dima);
}
