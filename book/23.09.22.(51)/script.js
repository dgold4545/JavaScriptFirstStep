'use strict';

let obj = {
    a: 1,
    b: 2,
    c: 3,
};
console.log(obj);
console.log(obj.a);

obj = {
    dima: 'diam',
};

console.log(obj.dima);

console.log(obj.b);

let date = {
    year: 2022,
    month: '09',
    day: 'friday',
};

alert(`${date.year}-${date.month}-${date.day}`);
