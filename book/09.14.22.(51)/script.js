'use strict';

let obj = {
    a: 1,
    b: 2,
    c: 3,
};

console.log(obj.a);

let date = {
    year: 2022,
    month: 'september',
    day: 'wednesday',
};

console.log(date);

console.log(date.year);
console.log(date.month, date.day);
console.log(date.year, date.month, date.day);
console.log('-', date.year, '-', date.month, '-', date.day, '-');
